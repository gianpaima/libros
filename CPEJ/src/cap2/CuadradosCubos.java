package cap2;

public class CuadradosCubos {
    public static void main(String[] args) {
        int a = 0;
        int b = 1;
        int c = 2;
        int d = 3;
        int e = 4;
        int f = 5;
        int g = 6;
        int h = 7;
        int j = 8;
        int k = 9;
        int l = 10;


        System.out.print("numero \t\tcuadrado \t\tcubo\n");
        System.out.printf("%s \t\t\t%s \t\t\t%s%n", a, (Math.pow(a,2)), (Math.pow(a,3)));
        System.out.printf("%s \t\t\t%s \t\t\t%s%n", b, (Math.pow(b,2)), (Math.pow(b,3)));
        System.out.printf("%s \t\t\t%s \t\t\t%s%n", c, (Math.pow(c,2)), (Math.pow(c,3)));
        System.out.printf("%s \t\t\t%s \t\t\t%s%n", d, (Math.pow(d,2)), (Math.pow(d,3)));
        System.out.printf("%s \t\t\t%s \t\t\t%s%n", e, (Math.pow(e,2)), (Math.pow(e,3)));
        System.out.printf("%s \t\t\t%s \t\t\t%s%n", f, (Math.pow(f,2)), (Math.pow(f,3)));
        System.out.printf("%s \t\t\t%s \t\t\t%s%n", g, (Math.pow(g,2)), (Math.pow(g,3)));
        System.out.printf("%s \t\t\t%s \t\t\t%s%n", h, (Math.pow(h,2)), (Math.pow(h,3)));
        System.out.printf("%s \t\t\t%s \t\t\t%s%n", j, (Math.pow(j,2)), (Math.pow(j,3)));
        System.out.printf("%s \t\t\t%s \t\t\t%s%n", k, (Math.pow(k,2)), (Math.pow(k,3)));
        System.out.printf("%s \t\t\t%s \t\t\t%s%n", l, (Math.pow(l,2)), (Math.pow(l,3)));



    }
}
